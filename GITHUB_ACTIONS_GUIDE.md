# 🚀 คู่มือใช้ GitHub Actions - Build Electron App

## 📋 ภาพรวม

GitHub Actions จะ **build อัตโนมัติ** สำหรับทุก platform:
- ✅ Windows (.exe)
- ✅ macOS (.dmg)
- ✅ Linux (.AppImage)

---

## 🎯 ขั้นตอนการใช้งาน

### ขั้นตอนที่ 1: สร้าง GitHub Repository

1. ไปที่ https://github.com/new
2. ตั้งชื่อ repository: `thumbnail-generator` (หรือชื่ออื่นที่ต้องการ)
3. เลือก **Public** (เพื่อใช้ Actions ฟรี)
4. **ไม่ต้อง** เลือก "Add README" หรือ ".gitignore" (เรามีอยู่แล้ว)
5. คลิก "Create repository"

### ขั้นตอนที่ 2: Push โค้ดไป GitHub

```bash
cd /var/www/gnok.dev/thumbnail-electron-app

# สร้าง Git repository
git init

# เพิ่มไฟล์ทั้งหมด
git add .

# Commit
git commit -m "Initial commit - Thumbnail Generator Electron App"

# เชื่อมต่อกับ GitHub (แทน YOUR_USERNAME ด้วยชื่อของคุณ)
git remote add origin https://github.com/YOUR_USERNAME/thumbnail-generator.git

# เปลี่ยนชื่อ branch เป็น main
git branch -M main

# Push ขึ้น GitHub
git push -u origin main
```

**หมายเหตุ**: ถ้า GitHub ขอ username/password ให้ใช้ **Personal Access Token** แทน password

### ขั้นตอนที่ 3: สร้าง Tag เพื่อ Trigger Build

```bash
# สร้าง tag เวอร์ชัน 1.0.0
git tag v1.0.0

# Push tag ขึ้น GitHub
git push origin v1.0.0
```

**เมื่อ push tag แล้ว GitHub Actions จะเริ่ม build อัตโนมัติทันที!**

### ขั้นตอนที่ 4: ตรวจสอบการ Build

1. ไปที่ GitHub repository ของคุณ
2. คลิกแท็บ **Actions**
3. จะเห็น workflow "Build Electron App" กำลังทำงาน
4. รอประมาณ **10-15 นาที**

### ขั้นตอนที่ 5: ดาวน์โหลดไฟล์

เมื่อ build เสร็จ:

1. ไปที่แท็บ **Releases** (ด้านขวาของหน้า repository)
2. คลิกที่ release "v1.0.0"
3. ดาวน์โหลดไฟล์ที่ต้องการ:
   - `Thumbnail Generator-1.0.0-portable.exe` (Windows)
   - `Thumbnail Generator-1.0.0.dmg` (macOS)
   - `Thumbnail Generator-1.0.0.AppImage` (Linux)

---

## 🔄 Build เวอร์ชันใหม่

เมื่อต้องการ build เวอร์ชันใหม่:

```bash
cd /var/www/gnok.dev/thumbnail-electron-app

# 1. แก้ไขโค้ด (ถ้ามี)
# ...

# 2. Commit การเปลี่ยนแปลง
git add .
git commit -m "Update: ..."
git push

# 3. อัปเดตเวอร์ชันใน package.json (ถ้าต้องการ)
# แก้ "version": "1.0.1"

# 4. สร้าง tag ใหม่
git tag v1.0.1
git push origin v1.0.1
```

GitHub Actions จะ build อัตโนมัติอีกครั้ง!

---

## 🛠️ การ Build แบบ Manual (ไม่ต้องรอ tag)

ถ้าต้องการ build ทันที โดยไม่ต้องสร้าง tag:

1. ไปที่ GitHub repository
2. คลิกแท็บ **Actions**
3. เลือก workflow "Build Electron App"
4. คลิกปุ่ม **Run workflow** (ด้านขวา)
5. เลือก branch `main`
6. คลิก **Run workflow**

---

## 📥 ดาวน์โหลดไฟล์จาก Actions (ถ้าไม่ใช้ Release)

ถ้า build แบบ manual:

1. ไปที่ **Actions**
2. คลิกที่ workflow run ที่เสร็จแล้ว
3. เลื่อนลงไปที่ **Artifacts**
4. ดาวน์โหลด:
   - `windows-build`
   - `mac-build`
   - `linux-build`

---

## 💡 เคล็ดลับ

### Personal Access Token (ถ้า GitHub ขอ password)

1. ไปที่ GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. คลิก "Generate new token (classic)"
3. ตั้งชื่อ: `Thumbnail Generator`
4. เลือก scope: `repo` (ทั้งหมด)
5. คลิก "Generate token"
6. **คัดลอก token** (จะแสดงครั้งเดียว!)
7. ใช้ token นี้แทน password เมื่อ git push

### ตรวจสอบ Build Log

ถ้า build ล้มเหลว:

1. ไปที่ **Actions**
2. คลิกที่ workflow run ที่ล้มเหลว
3. คลิกที่ job ที่ล้มเหลว (เช่น `build-windows`)
4. อ่าน error message
5. แก้ไขปัญหาและ push ใหม่

---

## 📊 ระยะเวลา Build

| Platform | ระยะเวลาโดยประมาณ |
|----------|-------------------|
| Windows | 5-8 นาที |
| macOS | 6-10 นาที |
| Linux | 3-5 นาที |
| **รวม** | **~15-20 นาที** |

---

## ✅ Checklist

- [ ] สร้าง GitHub repository
- [ ] Push โค้ดขึ้น GitHub
- [ ] สร้าง tag v1.0.0
- [ ] รอ build เสร็จ (~15 นาที)
- [ ] ดาวน์โหลดไฟล์จาก Releases
- [ ] ทดสอบแอปบนแต่ละ platform
- [ ] อัปเดต download page ด้วยลิงก์ใหม่

---

## 🎯 ขั้นตอนถัดไป

หลังจาก build เสร็จ:

1. **ดาวน์โหลดไฟล์** จาก GitHub Releases
2. **ทดสอบ** บนแต่ละ platform
3. **อัปโหลด** ไฟล์ไปที่ `/var/www/gnok.dev/downloads/`
4. **อัปเดต** download page ด้วยลิงก์ใหม่

---

**พร้อมแล้วครับ!** 🎉

ต้องการความช่วยเหลืออะไรเพิ่มเติมไหมครับ?
